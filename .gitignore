runs/
logs/
# MLflow file store. paths.mlflow_dir defaults to <repo>/mlruns, so the store lands
# inside the working tree; these are run artefacts, not source. Set TORCA_MLFLOW_DIR
# to move it out of the repo entirely.
mlruns/
.venv/
/data/
inspect_weights.py
pretrained/
.DS_Store
__pycache__/
ds/
scripts/runs/
!models/.gitkeep
*.safetensors
*.notes
*.csv
*.parquet
*.ipynb
.hf_birdmae_venv/
scripts/foundation_model/
.mlflowvenv
mlflow.db
compute_stats.py
check_pcen.py

# report_splits.py output — regenerated on demand, not a source artefact
splits.txt
report_splits.log
